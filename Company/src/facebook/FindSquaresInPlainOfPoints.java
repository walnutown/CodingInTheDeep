package facebook;

public class FindSquaresInPlainOfPoints {
   /*
    * Given a plain of points, find all the squares in the plain 
    */
   
   // [1] find all point pairs (i,j), this will take O(n^2)
   // [2] store distance(i,j) into a map, distance as key, list of point pairs as value
   // [3] traverse the map's keyset, for each key, find if there exist a square in the list of pairs
   // [4] Need to avoid duplicate results
}
