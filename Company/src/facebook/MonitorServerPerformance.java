package facebook;

public class MonitorServerPerformance {
   /*
    * Imagine you have 10,000 servers and need to monitor the performance of each one, how would you
    * do it
    */
   
   //[1] Each server maintain a log file, which will be sent to a master server periodically
   //[2] Master will analyze the logs and display a report
   //[3] We only need to collect logs periodically so as to save the bandwidth
}
