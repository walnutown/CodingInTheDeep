package ch16_threads_locks;

public class ThreadAndProcess {
   /*
    * What's the difference between thread and process
    */
   // Process:
   // [1] Each process has its own resources, including memory space
   // [2] each process have at least one thread and may have multiple threads
   // Thread:
   // [1] thread is a light-weight process
   // [2] threads in the same process share resources
}
